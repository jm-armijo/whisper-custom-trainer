# Environments and vendored clones (regenerable via setup.sh)
venv/
whisper/
whisper.cpp/

# Recorded audio is large and personal
data/
dataset.csv
# recorder_state.dataset_lock's sidecar, one per dataset
dataset.csv.lock

# Model artifacts are regenerable from dataset.csv
custom-lora-adapter/
merged-whisper-model/
exports/
scripts/

__pycache__/
*.pyc
.pytest_cache/

# TLS material: a private key must never be committed.
certs/
*.pem
