[pytest]
testpaths = tests
markers =
    unit: fast, isolated, no network or model downloads (default tier)
    integration: exercises real libraries and converters; downloads the base model
    e2e: full pipeline through a trained adapter to transcribed text; slow
# Unit tests are the default run so the feedback loop stays fast.
addopts = -m "not integration and not e2e"
